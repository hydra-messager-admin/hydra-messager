# hydra-messager
Asynchronous Zero-Knowledge E2EE messenger node built with Django, Channels, and Redis. Features client-side Web Crypto API (AES-GCM) authentication via Challenge-Response protocol and ephemeral, RAM-only volatile chat history with strict TTL.
